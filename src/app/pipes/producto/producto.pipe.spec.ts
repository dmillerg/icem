import { ProductoCategoriaPipe } from './producto.pipe';

describe('ProductoCategoriaPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductoCategoriaPipe();
    expect(pipe).toBeTruthy();
  });
});
